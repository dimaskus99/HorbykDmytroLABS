services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: 13brh1b3rbuy31gbruy3b
      FLASK_APP: app
      FLASK_ENV: development
      DATABASE_URL: "postgresql://dimas:dimas88@db/lab3_db"
    depends_on:
      - db
    volumes:
      - .:/app

  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: dimas
      POSTGRES_PASSWORD: dimas88
      POSTGRES_DB: lab3_db
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
